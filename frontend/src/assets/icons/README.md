# Iconos del sistema

Coloca aqui tus iconos propios en formato WEBP o SVG. Para reemplazar uno existente, usa el mismo nombre de archivo, por ejemplo `carrito.webp`, `cliente.webp` o `search.svg`.

Si agregas un icono nuevo, importalo en `src/components/AppIcon.jsx` y sumalo al objeto `iconFiles`.
