services:

  mysql:
    image: patracamiguel/av-db:5.1
    container_name: av-db

    ports:
      - "3308:3306"

    restart: always

  backend:
    build: ./backend/proyecto
    container_name: av-backend

    ports:
      - "8083:8083"

    depends_on:
      - mysql

    restart: always
