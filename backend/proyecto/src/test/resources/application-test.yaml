spring:
  datasource:
    url: jdbc:h2:mem:av_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  security:
    user:
      name: admin
      password: admin123

app:
  jwt:
    secret: AVPublicidadSecretKeyForJwtAuthChangeMe123456
    expiration-millis: 86400000
